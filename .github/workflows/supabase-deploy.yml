name: Deploy Supabase

# Auto-deploys Supabase edge functions whenever main changes anything under
# supabase/functions/. Also exposes a manual "Run workflow" button as a
# fallback (workflow_dispatch).
#
# NOTE: Migration auto-apply (supabase db push) is intentionally disabled.
# The repo's migration history has a pre-existing filename collision at
# 20260417000001 (two files share that version) and accumulated drift
# between local files and remote tracking. Migrations are currently applied
# manually via MCP after merge. See Followups in docs/hr-roadmap.md.
#
# Required repo secrets:
#   SUPABASE_ACCESS_TOKEN — personal access token (https://supabase.com/dashboard/account/tokens)
#   SUPABASE_DB_PASSWORD  — project's DB password (Database → Settings → Reset password)
#
# Project ref is hardcoded below since it's not secret.

on:
  push:
    branches: [main]
    paths:
      - 'supabase/functions/**'
      - '.github/workflows/supabase-deploy.yml'
  workflow_dispatch:

permissions:
  contents: read

# Don't let two deploys run at the same time.
concurrency:
  group: supabase-deploy
  cancel-in-progress: false

env:
  SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
  SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
  PROJECT_REF: jpaihltkrohdqkqlbqkf

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link to project
        run: supabase link --project-ref "$PROJECT_REF"

      - name: Deploy edge functions
        run: |
          for dir in supabase/functions/*/; do
            fn=$(basename "$dir")
            # Skip shared/private directories (convention: leading underscore)
            if [[ "$fn" == _* ]]; then
              echo "Skipping $fn (shared/private)"
              continue
            fi
            echo "::group::Deploying $fn"
            supabase functions deploy "$fn"
            echo "::endgroup::"
          done
